# ============================================
# 量化 AI 进化系统 — Python 依赖
# ============================================
# 安装：pip install -r requirements.txt
# ============================================

# --- 数据获取 ---
akshare>=1.16.0           # A股/宏观数据接口（免费开源）

# --- 回测引擎 ---
vectorbt>=0.5.0           # 极速向量化回测
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# --- LLM API ---
openai>=1.0.0             # OpenAI 兼容客户端（用于 DeepSeek API）

# --- 数据存储与图表 ---
matplotlib>=3.7.0
seaborn>=0.12.0
python-dotenv>=1.0.0     # .env 环境变量加载

# --- 工具库 ---
rich>=13.0.0             # 终端美化输出
tqdm>=4.65.0             # 进度条
pyyaml>=6.0              # YAML 配置解析
tabulate>=0.9.0          # 表格格式化输出